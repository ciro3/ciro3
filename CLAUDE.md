# CLAUDE.md — ciro3/ciro3

## What This Repository Is

This is Jose Garcia's **GitHub profile repository** (`ciro3/ciro3`). GitHub automatically renders the `README.md` from a repository whose name matches the owner's username as the public-facing profile page at [github.com/ciro3](https://github.com/ciro3).

There is no application code, build system, test suite, or infrastructure here. The sole artifact is `README.md`.

## Repository Structure

```
ciro3/
└── README.md   # GitHub profile landing page (rendered on github.com/ciro3)
```

## Owner Background

- **Name:** Jose Garcia
- **Location:** British Columbia, Canada
- **Background:** Environmental science and engineering (Environmental Management Professional)
- **Current focus:** Learning to code (CS50 course)
- **Interests:** Environmental and sustainability projects
- **Socials:** GitHub ([ciro3](https://github.com/ciro3)), LinkedIn ([josecgb3](https://www.linkedin.com/in/josecgb3/))

## Development Workflow

All meaningful changes are edits to `README.md`. The typical workflow is:

1. Edit `README.md` directly
2. Commit with a descriptive message (`git commit -m "Add skills section"`)
3. Push to `main` (`git push origin main`)

Changes pushed to `main` appear live on the GitHub profile page within seconds.

## Conventions for README Updates

- Keep the tone personal, concise, and authentic
- Prefer Markdown over raw HTML where possible; use HTML only for features Markdown cannot handle (e.g. centered images, badge alignment)
- Social icons currently use the `danielcranney/readme-generator` CDN — keep that pattern consistent for any new icons
- Do not add sections that don't reflect Jose's actual current state (e.g. don't add a "Skills" section with technologies he hasn't used)
- Emoji usage is intentional and welcome — it matches the existing style

## Git History Summary

All 11 commits (as of June 2026) are README-only updates. No source code has ever been committed. The commit style used is simply `"Update README.md"` for most changes.

## Notes for AI Assistants

- There is no build step, no `npm install`, no test runner — do not attempt to run or lint code
- The only file that matters for the user's goal is `README.md`
- When asked to improve or expand the profile, base suggestions on Jose's stated background (environmental science + learning to code) rather than inventing credentials
- Do not add GitHub stats widgets, streak counters, or third-party dynamic badges without being asked — these add external dependencies and maintenance burden
- This repo may grow into an actual project repository over time; if source code is added, update this file accordingly
