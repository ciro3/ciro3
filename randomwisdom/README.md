# RandomWisdom

A simple command-line tool that prints a random wisdom quote to your terminal.

## Usage

```bash
python3 randomwisdom.py
```

Print multiple quotes at once:

```bash
python3 randomwisdom.py -n 3
```

## Requirements

Python 3.7+. No external dependencies.
